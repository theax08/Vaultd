node_modules/
build/
.env
.env.*
.DS_Store
npm-debug.log*
.react-router/
prisma/*.sqlite
prisma/*.sqlite-journal

# Contains local secrets (DB passwords, admin API secrets) baked into
# permission allow-lists — never commit.
.claude/
.cursor/
.gemini/
